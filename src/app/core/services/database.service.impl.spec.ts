import { DatabaseServiceImpl } from './database.service.impl';

describe('DatabaseServiceImpl', () => {
  it('should create an instance', () => {
    expect(new DatabaseServiceImpl()).toBeTruthy();
  });
});
